--Compile Instructions--
1.The project is in a package, so it requires javac -d "PA2" *.java to compile.
  This ensures its class files are within the correct directory for a package
2.To run, java -cp "PA2" PA2.PA2 test.dat ensures correct classpath, with test.dat in the root